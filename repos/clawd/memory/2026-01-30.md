# 2026-01-30

## 設定変更
- タイムゾーンを日本時間 (Asia/Tokyo) に変更 (`agents.defaults.userTimezone`)
- 起動時挨拶機能を HEARTBEAT.md + heartbeat-state.json で実装
  - Slack C0ABC66S869 に挨拶を送る設定
  - `startupAnnounced` フラグで重複防止

## 学んだこと
- cronジョブには `@startup` トリガーがない
- 起動時タスクは HEARTBEAT.md + フラグファイルで代用可能
- 定期実行や時刻指定が必要な場合のみcronを使う
